<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
  <key>Label</key>
  <string>com.trevor.ai-warm</string>

  <key>ProgramArguments</key>
  <array>
    <string>__HOME__/.local/bin/ai-warm</string>
    <string>once</string>
  </array>

  <key>EnvironmentVariables</key>
  <dict>
    <key>PATH</key>
    <string>__HOME__/.local/bin:/opt/homebrew/bin:/usr/bin:/bin:/usr/sbin:/sbin</string>
  </dict>

  <!-- Run from a local dir so the CLIs don't scan for project config starting
       at "/" and wander onto mounted network volumes (macOS privacy prompt). -->
  <key>WorkingDirectory</key>
  <string>__HOME__</string>

  <!-- Fire every 5 hours to keep both rolling usage windows cycling. -->
  <key>StartInterval</key>
  <integer>18000</integer>

  <!-- Tick once at load (login / reboot) so the windows start immediately. -->
  <key>RunAtLoad</key>
  <true/>

  <key>StandardOutPath</key>
  <string>__HOME__/.local/state/ai-warm.log</string>
  <key>StandardErrorPath</key>
  <string>__HOME__/.local/state/ai-warm.log</string>
</dict>
</plist>
